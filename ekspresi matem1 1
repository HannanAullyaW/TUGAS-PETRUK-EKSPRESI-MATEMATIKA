#include <iostream>
#include  <string>
#include <cstring>
using namespace std;

int main (){
    string kata;
    getline(cin,kata);    
    cout<<"Print : ";
	int N =  kata.length();
    for(int x = 0; x < N ; x++){
        if(kata[x] == '-' && (kata[x-1] == '+' || kata[x-1] == '-' ||kata[x-1] == '*' || kata[x-1] == '/'|| kata[x-1] == '%' )){
            cout << "-1 * ";
        }
        
        return 0;
 }
